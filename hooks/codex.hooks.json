{
  "description": "Codex project hook registration for the context-zone Stop hook.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$(git rev-parse --show-toplevel)/.codex/hooks/context-zone.sh\"",
            "timeout": 30,
            "statusMessage": "Checking context zone"
          }
        ]
      }
    ]
  }
}
